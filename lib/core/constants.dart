const double kDouble5 = 5.0;
const double kDouble10 = 10.0;
const double kDouble20 = 20.0;
const double kDouble40 = 40.0;

const String descriptionPage = 'In your provided code, it seems that you\'ve set specific widths for your <th> and <td> elements, but the widths of the <th> and <td> elements may differ due to various factors such as content and padding. To ensure that the <th> and <td> elements have the same width, you can do the following: Equalize Widths: Set the same width for the corresponding <th> and <td> elements in your table. This can be done by specifying a fixed width using CSS. Make sure to specify the width for both the <th> and the <td>. Apply Box-Sizing: Use the box-sizing CSS property to ensure that the padding is included in the specified width. By default, the box-sizing property is set to content-box, which doesn\'t include padding in the specified width. Here\'s an example of how you can modify your CSS to ensure that <th> and <td> elements have the same width:';